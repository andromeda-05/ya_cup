#include <iostream>
#include <vector>
#include <stack>
 
using std::string;
using std::cin;
using std::cout;
 
int main() {
    int N,X,K;
    cin >> N >> X >> K;
    
 
    int result = 0;
    for (const char ch : s) {
        result += j.find(ch) != string::npos;
    }
 
    cout << result;
 
    return 0;
}
